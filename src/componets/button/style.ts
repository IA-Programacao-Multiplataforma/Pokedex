import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        width: '100%',
        height: 52,
        backgroundColor: '#0c1485',
        borderRadius: 8,
        justifyContent: 'center',
        alignItems: 'center',   
    },
    title: {
        color: '#fff',
        fontSize: 16,
        fontWeight: 'bold',
    }
}); 